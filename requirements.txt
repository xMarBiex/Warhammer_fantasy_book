# --- Ekstrakcja i OCR (krok 1-2) ---
pymupdf>=1.24        # odczyt PDF, warstwa tekstowa, render stron do obrazów
pytesseract>=0.3.10  # wrapper na tesseract OCR (wymaga systemowego tesseract-ocr + pol/eng)
pillow>=10.0

# --- Warstwa wektorowa / semantyczna (Warstwa 4 KOS) ---
chromadb>=1.5        # trwała baza wektorowa (cosine)
fastembed>=0.8       # embeddingi ONNX (bez torcha); model multilingual-e5-large
# Uwaga: HuggingFace bywa niedostępny; model E5-large pobierany jest z
# Google Cloud Storage (patrz scripts/embedder.py). Instaluj w venv,
# by uniknąć konfliktu z systemowym PyYAML (Debian).

# --- Agent / Reasoning Engine (Warstwa 5 KOS) ---
# Backend domyślny: lokalny Bielik przez Ollamę (agent/ollama_agent.py) — mówi
# do REST API Ollamy przez stdlib urllib, bez dodatkowej zależności pip.
# Wymaga zainstalowanej Ollamy (ollama.com) i pobranego modelu:
#   ollama pull SpeakLeash/bielik-11b-v3.0-instruct:Q4_K_M
anthropic>=0.69      # opcjonalnie: backend Claude API (KOS_BACKEND=claude)
# Warstwa Graf+SQL (Warstwa 2+3) używa sqlite3 ze stdlib — bez zależności.
