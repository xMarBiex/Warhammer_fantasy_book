# Dane i artefakty generowane — NIE trzymamy w gicie
# (trzymamy tylko strukturę katalogów przez .gitkeep)
data/*
!data/raw/
data/raw/*
!data/**/.gitkeep

# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Lokalna konfiguracja (klucz API, hasło) — NIGDY w gicie
config.bat

# Modele / cache embeddingów
.cache/
models/

# Spakowane archiwum danych (generowane przez scripts/package_data.sh)
*.tar.gz
kosdane.part*

# Narzędzia pobierane osobno (nie są kodem projektu)
cloudflared.exe

# Lokalna konfiguracja Claude Code (uprawnienia, launch config per maszyna)
.claude/

# Wygenerowana strona mobilna (zawiera treść księgi = dane)
web/search_mobile.html

# Paczka do wdrożenia (generowana, zawiera treść księgi)
dist/
*.zip
